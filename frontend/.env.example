# Copy to frontend/.env.local for dev overrides.

# Empty in dev - Vite proxies /api/* to localhost:8000.
# In prod, set this to the absolute URL of the deployed Django API.
VITE_API_BASE_URL=

# Optional: name shown in <title> and the navbar.
VITE_APP_NAME=GrindMate
