# Runtime dependencies - used in dev, staging, and production.
# Pins are minimum-version floors; pip resolves the latest compatible release.

# --- Core --------------------------------------------------
Django>=5.2,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-environ>=0.11
django-filter>=24.3

# --- Database ----------------------------------------------
psycopg[binary]>=3.2
dj-database-url>=2.3

# --- Background jobs ---------------------------------------
celery[redis]>=5.4
django-celery-beat>=2.7
django-celery-results>=2.5
redis>=5.0

# --- HTTP / external APIs ----------------------------------
requests>=2.32
tenacity>=9.0

# --- Production server / static files ----------------------
gunicorn>=23.0
whitenoise>=6.7
